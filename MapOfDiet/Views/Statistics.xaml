<UserControl x:Class="MapOfDiet.Views.Statistics"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MapOfDiet.ViewModels"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:StatisticsViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <StackPanel>
            <!-- Заголовок -->
            <TextBlock Text="{Binding Title}" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Margin="0,0,0,20"/>

            <!-- Кнопка обновления -->
            <Button Content="Обновить статистику"
                    Command="{Binding RefreshCommand}"
                    Margin="0,0,0,20"
                    Width="200"
                    HorizontalAlignment="Left"/>

            <!-- Таблица статистики -->
            <DataGrid ItemsSource="{Binding Last7Days}" 
                      AutoGenerateColumns="False" 
                      HeadersVisibility="Column" 
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Margin="0,0,0,20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата" Binding="{Binding Date}" Width="*"/>
                    <DataGridTextColumn Header="Цель (ккал)" Binding="{Binding TargetCalories}" Width="*"/>
                    <DataGridTextColumn Header="Факт (ккал)" Binding="{Binding ActualCalories}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
