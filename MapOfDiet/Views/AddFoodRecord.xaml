<UserControl 
    x:Class="MapOfDiet.Views.AddFoodRecord"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    xmlns:conv="clr-namespace:MapOfDiet.Converters"
    mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="9*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <TextBox Style="{StaticResource SearchTextBoxStyle}" Text="{Binding SearchNameFood}" FontSize="18" VerticalContentAlignment="Center" Margin="5" />
            <Button Content="Поиск" Command="{Binding SearchFoodCommand}" FontSize="18" Grid.Column="1" Margin="10"/>
        </Grid>



        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Дата:" VerticalAlignment="Center" FontSize="20" Margin="5" Grid.Column="0"/>
            <DatePicker SelectedDate="{Binding DateFood, Mode=TwoWay}" VerticalAlignment="Center" FontSize="16" Margin="5" Grid.Column="1"/>

            <TextBlock Text="Время:" VerticalAlignment="Center" FontSize="20" Margin="5" Grid.Column="2"/>
            <xctk:TimePicker Value="{Binding TimeFood, Mode=TwoWay}" Format="ShortTime" VerticalAlignment="Center" FontSize="16" Margin="5" Grid.Column="3"/>
        </Grid>

        <ListBox Grid.Row="2" ItemsSource="{Binding SearchResultsFood}" Margin="0,10,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="10" Margin="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>

                            <Image Source="{Binding Image, Converter={StaticResource ByteArrayToImageConverter}}" Grid.Column="0" Width="80" Height="80" Stretch="UniformToFill" Margin="5"/>
                            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0">
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Calories}" FontSize="14" Foreground="Gray"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="Масса:" VerticalAlignment="Center"/>
                                    <TextBox Text="{Binding EnteredMass, Mode=TwoWay}" Width="60" Margin="5,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                            <Button Grid.Column="2" Content="Добавить" Command="{Binding DataContext.AddFoodCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" VerticalAlignment="Center" Margin="10"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
